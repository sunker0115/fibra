format: 1
id: fibra-tool-fs-search
version: "${project.version}"
facets:
  - id: main
    role: host
    runtime: java
    target: host
    payload: lib/main.jar
    dependencies:
      - pluginId: fibra-subprocess
        facetId: main
    capabilities: []
