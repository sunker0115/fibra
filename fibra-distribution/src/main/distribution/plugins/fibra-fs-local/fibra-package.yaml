format: 1
id: fibra-fs-local
version: "${project.version}"
facets:
  - id: main
    role: host
    runtime: java
    target: host
    payload: lib/main.jar
    dependencies:
      - pluginId: fibra-fs
        facetId: main
    capabilities: []
