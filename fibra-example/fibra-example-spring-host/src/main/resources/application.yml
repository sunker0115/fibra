fibra:
  artifacts:
    installed-root: ./run/plugins
    watch:
      enabled: false
  config:
    location: ./run/fibra.yaml
    watch:
      enabled: false
  example:
    staging-root: ./run/staging

spring:
  servlet:
    multipart:
      max-file-size: 32MB
      max-request-size: 32MB
